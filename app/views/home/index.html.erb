


  <div class="main-container-hero">
      <h1>Teacher Lesson Plans<h1>
        <p class="key-words">find, create, share, connect</p>
        <p class="third-line">all in one place</p>
    </div>

  <a id="steady_scroll" name="steady_scroll"></a>
    <div class="search-container">
      <%= render :partial => 'home/search' %>
    </div>
  </a>

  <a id="cards_scroll" name="cards_scroll">
  <div class="card-container">
    <div class="cards">
    <% @lessons.each do |l| %>
      <div class="card">
        <%= link_to l do %>
          <div class="card-image">
            <h3> <%= l.topic %></h3>
          </div>
          <div class="card-header">
            <%= l.description.html_safe + "..." %>
          </div>
          <div class="card-copy">
          </div>
        <% end %>
      </div>
      <% end %>
    </div>
  </div>
 </div>
</a>
