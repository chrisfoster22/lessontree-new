  <%= form_tag '/' + '#cards_scroll', :method => 'get' do %>
    <%= text_field_tag :search, params[:search], :id => 'search_field' %>
    <%= select_tag "Subject", options_from_collection_for_select(Subject.all, "id", "title") %>
    <%= submit_tag "Search", :name => nil %>
<% end %>
